测试编写建议：
- rerank的生成数据任务处理流程和训练任务处理流程需要增加单测覆盖，可以参考@test\cases\service\core\evaluation\task\中对任务流程的测试
- 其他长流程或重要函数的测试需要补全，保证关键流程符合期望
- 额外增加端到端测试，编写测试脚本验证 API 功能
    - 使用 `make dev name=app` 在根目录拉起服务
    - 编写测试脚本发起 HTTP 请求
    - 验证响应结果是否符合预期

**note**: 基于对应的schema构造真实数据进行测试



逻辑待完善：
rerankTrainDataGenerateProcessor的处理过程，本质上是调用多个对每个关联的知识库训练集下发`生成任务（长时任务）`, 但generateAppTrainsetDataCore中的处理是下发任务后，马上去判断是否就绪，如果不就绪，抛出错误（由队列重试），这样的设计是有问题的，当各个知识库训练集都不存在时，应用训练数据生成必定会在超过队列重试次数后失败；

generateAppTrainsetDataCore不应该下发任务后直接返回，而是原地轮询直到



新需求：重构训练集的结构，移除知识库级别训练集的维护
背景：训练集和APP应该是1:1的数据关系，训练集的生成是基于和APP关联的所有知识库分片而非单个知识库(和负样本挖掘算法有关)
要求：
- 本次改动无需考虑向后兼容，直接删除对应的知识库级别训练集schema和相关的用法；
- 关联修改相关设计文档
- 关联修改相关测试